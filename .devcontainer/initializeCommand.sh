#!/bin/bash

# SERVER="gitlab-intern.ait.ac.at"
# PORT=5010

# # Check if the server is available
# if nc -z -v -w5 $SERVER $PORT; then
#   echo "Server is available"
#   docker pull $SERVER:$PORT/vac-cds/projects/2019_lp_autonome_baustelle_grp/ros2_baustelle_ws:master || true
# else
#   echo "Server is not available"
# fi
